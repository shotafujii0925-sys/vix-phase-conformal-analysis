# 公開前修正 最終報告書

前回の総合評価で指摘した全項目への対応記録。**全ての数値は同梱スナップショット（`data/vix_sp500_tnx_raw.csv`、2026-09-08取得）を用いた再実行結果であり、オンライン再取得は行っていない。**

---

## 1. 元レビューの指摘一覧と対応

### 公開前に修正必須（6件）

| # | 指摘 | 対応 | 状態 |
|---|---|---|---|
| 1 | 図のファイル名衝突（`analysis_05_figure.py` と `vix_analysis.py` が同じ `vix_phase_returns_fixed.png` に異なる内容を書く）。README 8章の説明が実物と不一致 | 出力名を `phase_returns_daily_vs_thinned.png`（2パネル）と `phase_returns_overview.png`（1パネル）に分離。README は実物に合わせて記述。衝突検査テストを追加 | **修正完了** |
| 2 | スナップショットCSVが書き出されるだけで読まれない。「再現性を確保している」は事実に反する | `build_dataset(source="snapshot")` を**既定**に変更しCSVから読む実装を追加。`--source online` を明示した場合のみ再取得。テストで検証 | **修正完了** |
| 3 | LOCO章が、4〜5章で自ら確立した「日次重複p値は過大」という批判を自分に適用していない（p=5.08e-12を根拠にしていた） | p値を根拠として使わない旨をコードの出力とドキュメントに明記。**エピソード単位の符号検定を新規実装**（9個中8個、p=0.0391）。主要な発見・結論の表現を変更 | **修正完了** |
| 4 | ACI vs Fixed の比較が交絡（ACIは適応α＋ローリング窓、Fixedは21年間無更新） | **第3の手法 RollingFixed（ローリング窓＋固定α）を追加**し、4手法比較に変更。「αの適応の効果」と「窓の更新の効果」を分離。Cが極端な設定であることを明記 | **修正完了** |
| 5 | 僅差を順位として断定（不確実性の定量化がゼロ） | **moving block bootstrap を実装**（B=1000、ブロック長 max(21, 2h)）。Winkler差・カバレッジ差に95%CIを付与。CIが0をまたぐ場合は「明確な差なし」と記述 | **修正完了** |
| 6 | 「先読みバイアスなし」が厳密には誤り（学習窓末尾h行のtargetが未実現） | `feat.iloc[:start]` → `feat.iloc[:start - h + 1]` に修正。ACI初期履歴からも末尾h件を除外。自動テストで検証。文書の表現も修正 | **修正完了** |

### 公開後でも改善可能（8件）

| # | 指摘 | 対応 | 状態 |
|---|---|---|---|
| 7 | ACIのα更新式が原論文と不一致（現在のqで過去残差を判定、クリップ後の区間で判定していない） | 発行済み区間をキューに保存し、**発行時点のクリップ後区間で被覆判定**するよう書き換え。テスト2件で検証。原論文との差分（ローリング窓・下限クリップ）を文書に明記 | **修正完了** |
| 8 | 効果量・Nのハードコード（analysis_03、vix_analysis §2.1） | `results_01_phase_tests.json` から読み込む実装に変更。vix_analysis は直前に計算した `thin_df` から取得 | **修正完了** |
| 9 | テストがゼロ | **24件のテストを追加**（外部API接続なし） | **修正完了** |
| 10 | git未使用・`.gitignore`なし | `.gitignore` を追加（Python/pytest/OS/IDE/.env）。**git initとpushは指示通り実行していない** | **部分対応** |
| 11 | `tabulate` が未使用 | requirements.txt から削除。pytest/flake8 を追加 | **修正完了** |
| 12 | 相対パス直書き12箇所 | `data_pipeline.py` に `REPO_ROOT` / `DATA_DIR` / `FIGURES_DIR` / `DOCS_DIR` を定義し全スクリプトで使用 | **修正完了** |
| 13 | `_safe_savefig` のdocstringが誤った原因（ウイルススキャン）を記載 | 真因（Windows MAX_PATH 260文字）を明記し、リトライではなくパス長の事前検査に変更 | **修正完了** |
| 14 | `build_dataset()` の二重呼び出し、`gbm_test` 未定義の可能性 | `main()` に `df_raw` 引数を追加して二重読み込みを解消。`gbm_test is None` 時に明示的な例外を送出 | **修正完了** |

### 「中」「小」で指摘した項目

| # | 指摘 | 対応 | 状態 |
|---|---|---|---|
| 15 | 境界的p値の太字強調（Kruskal p=0.0450、金利差分 p=0.0499） | 太字を解除。「多重比較未補正の境界値であり、探索的結果として位置づける」と明記 | **修正完了** |
| 16 | 多重比較の総量が未管理 | 「多重比較について」節を追加し、検証的仮説群（Holm補正済み）と探索的分析群を区別。機械的な一律補正を適用しない理由を記載 | **修正完了** |
| 17 | 再学習間隔252日の根拠が不明 | 「計算コストとの妥協で固定、感度分析は未実施」とコード・文書に明記 | **修正完了（正直な記述）** |
| 18 | 休日・欠損の処理が未文書化 | データ検証関数を実装し実行時に出力。詳細レポートに表で記載 | **修正完了** |
| 19 | `shift(-126)` が暦日6ヶ月ではない | コード・README・詳細レポートに明記 | **修正完了** |
| 20 | GBMのハイパーパラメータの根拠がない | 「計算時間を抑えるため事前に固定、探索は一切なし、評価期間は不参照」と明記。チューニング不足の可能性を排除できない旨も記載 | **修正完了（正直な記述）** |
| 21 | README 47KB・560行が長すぎる | READMEを**3分要約中心の構成に全面書き換え**。詳細は `docs/detailed_report.md` と `docs/key_results.md` へ分離 | **修正完了** |
| 22 | 章番号が不自然（9章の後に9.5章、その後10章） | 前半/後半の2部構成に整理し、9.5章を廃止。接続理由を明記 | **修正完了** |
| 23 | 前半と後半の接続が「視点を変え」の一言 | 「前半で説明力が1%未満と判明したため、方向の点予測ではなく不確実性の区間評価へ問題を移した」と明記 | **修正完了** |
| 24 | READMEの数値が手作業転記 | `build_key_results.py` で `docs/key_results.md` を自動生成。READMEの主要数値とJSONの一致をテストで検査 | **修正完了** |
| 25 | 「配当込みなら概ね+0.5〜1pt」が未計算の数値 | 記述を削除し、「定量化は未実施」と明記 | **修正完了** |
| 26 | h=21のpre/post逆転が未検証の仮説のまま | **対象日(target date)基準での再集計を実装して検証**。逆転が解消することを確認（ACI: pre 85.8% → post 43.2%） | **修正完了** |
| 27 | 危機エピソードのNが小さいことが未明示 | 全ての集計を `n_covered/n` 形式（例 7/21）で出力。標準誤差が約10ptあることを明記 | **修正完了** |
| 28 | 危機発生日の定義（Panic初日）の限界 | 「市場ストレスはVIX>40より前から始まっている可能性」を明記。pre時点で既にカバレッジが低い事実がそれを示唆すると記載 | **修正完了** |
| 29 | AI利用が非開示 | README に「AI利用について」節を追加。本人担当分とClaude担当分を明示 | **修正完了** |
| 30 | 周辺カバレッジと条件付きカバレッジの区別が未記載 | コードのコメントと文書に明記 | **修正完了** |

### 修正不要と判断した項目

| 指摘 | 判断理由 |
|---|---|
| サブ期間分割（1990-2007 / 2008-2023）の追加 | **新しい分析の追加**にあたり、今回の制約「後付け分析を増やさない」に反する。研究上の限界として「単一期間」と明記し、今後の課題に記載した |
| 取引コスト・戦略バックテストの追加 | 同上。新しい研究テーマになる。限界として明記済み |
| 目的変数をΔVIXへ変更 | 指示で明示的に禁止されている。今後の課題に記載 |
| 配当込みトータルリターンでの再計算 | 新しいデータソース（^SP500TR）の追加が必要。限界と今後の課題に記載 |
| ACIハイパーパラメータ（γ、較正窓）の感度分析 | 実施すれば「ACIが良く見える設定を探す」誘因が生じる。今回は固定値を明示し、感度分析未実施と正直に記載する方針を選んだ |
| h=1のVIX予測という課題設定自体 | 研究の前提であり、変更は新テーマ。「課題として易しい」という限界を明記した |

---

## 2. 変更したファイル一覧

| ファイル | 変更内容 |
|---|---|
| `data_pipeline.py` | **全面改修**: スナップショット読み込み（既定）、pathlib、データ検証関数、重複日付検出、`find_vix_episodes` の共通化、CLI引数 |
| `conformal_vix_forecast.py` | **全面改修**: 先読み修正、ACI更新式の修正、4手法比較、ブロックブートストラップ、対象日基準の再集計、図名変更、`df_raw` 引数、パス長検査 |
| `analysis_01_phase_tests.py` | pathlib化、未使用import削除 |
| `analysis_02_leave_one_crisis_out.py` | **エピソード単位の符号検定を追加**、p値の解釈に関する警告出力、pathlib化 |
| `analysis_03_hac_power.py` | 効果量・NをJSONから読み込むよう変更、pathlib化 |
| `analysis_04_stationarity_economic.py` | pathlib化、ハードコードされた数値のコメント削除、未使用import削除 |
| `analysis_05_figure.py` | 図名変更、Aggバックエンド指定、pathlib化、未使用import削除 |
| `vix_analysis.py` | 図名変更、ハードコード解消、`df_raw` の受け渡し、4手法対応の図に変更 |
| `build_key_results.py` | **新規**: JSONから `docs/key_results.md` を自動生成 |
| `tests/test_repo.py` | **新規**: 24件のテスト |
| `README.md` | **全面書き換え**: 3分要約中心、詳細は docs/ へ分離、AI利用開示を追加 |
| `docs/detailed_report.md` | **新規**: 手法・検証・解釈の詳細 |
| `docs/key_results.md` | **新規（自動生成）**: 全数値表 |
| `.gitignore` | **新規** |
| `setup.cfg` | **新規**: flake8 / pytest 設定 |
| `requirements.txt` | tabulate削除、pytest・flake8追加 |
| 削除 | `figures/vix_phase_returns_fixed.png`（衝突していた図） |

---

## 3. 先読み修正の内容

**修正前**

```python
train = feat.iloc[:start]   # 再学習時点 start までの全行
```

行 i の target は i+h 時点で実現する。したがって末尾h行（i = start-h 〜 start-1）のtargetは、再学習時点 `start` ではまだ実現していない。h=1で1行、h=21で21行が該当。

**修正後**

```python
train_end = start - h + 1   # targetが実現済みの行までに限定
train = feat.iloc[:train_end]
```

加えて、ACIの初期履歴も較正期間の残差から末尾h件を除外した（同じ理由で、末尾h件のtargetは評価期間側で実現するため）。

**検証**: `test_walk_forward_training_targets_are_realized[1]` / `[21]` が、全ての再学習時点について「学習末尾行のtarget実現時点 ≤ 再学習時点」を検査する。

**影響**: h=1のGBM MAE 1.359 → **1.369**（1行減の影響）。h=21のGBM MAE 4.496 → **4.661**（21行減の影響）。結論の方向は変わらない。

---

## 4. ACI更新ロジックの変更内容

**修正前**

```python
err = 1.0 if newly_known > q else 0.0   # 現在時刻のqで、h日前の残差を判定
```

2つの誤りがあった。
1. 判定に使う `q` が、その予測を発行した時点のものではなく現在時刻のもの
2. 下限を0にクリップした「実際に発行した区間」ではなく、残差とqの大小で判定

**修正後**

```python
pending.append({"lo": lo, "hi": hi, "y": target[i], "resid": resid_abs[i]})
if len(pending) > h:
    done = pending.pop(0)                          # h ステップ前に発行した区間
    history.append(done["resid"])
    covered = (done["lo"] <= done["y"] <= done["hi"])   # 発行時の、クリップ後の区間で判定
    err = 0.0 if covered else 1.0
    alpha_t = clip(alpha_t + gamma * (alpha_target - err))
```

**検証**: 4件のテストで確認。
- `test_aci_feedback_is_delayed_by_h`: 最初のh+1ステップでαが動かない
- `test_aci_uses_issued_clipped_interval_for_coverage`: `pred - q < 0` のケースで、クリップ後の区間が被覆していればαが増える（修正前は誤って「外れ」と判定していた）
- `test_aci_alpha_moves_down_when_missing`: 外れ続けるとαが下がる
- `test_pending_feedback_has_no_loss_or_duplication`: キューの取りこぼし・二重処理がない

**原論文との差分（文書に明記済み）**: αの更新則は Gibbs & Candes (2021) と同形式だが、(a) 全履歴ではなく直近500件のローリング窓から分位点を取る、(b) 下限を0でクリップする、という2点で変種である。したがって「**ACIの考え方に基づく実装**」であり「原論文を厳密に再現した」とは主張しない。

---

## 5. 比較手法の変更内容

**修正前**: ACI（ローリング窓＋適応α） vs FixedSplit（固定） vs Naive の3手法。ACIとFixedの差が「αの適応」なのか「窓の新しさ」なのか分離できていなかった。

**修正後**: 4手法。点予測モデル・期間・情報集合・下限クリップ・評価指標・危機定義を全て統一。

| 手法 | 残差窓 | alpha |
|---|---|---|
| A. ACI | ローリング500日 | 適応 |
| B. RollingFixed | ローリング500日 | 固定0.10 |
| C. FixedSplit | 較正期間で固定 | 固定0.10 |
| D. NaiveBaseline | 較正期間で固定（モデルなし） | 固定0.10 |

- **A vs B** = αを適応させる効果
- **B vs C** = 最近の残差で分位点を更新する効果
- **A vs D** = 全体としての効果

Cが「21年間再較正しない」極端な設定であり、非適応手法一般の代表ではないことを文書に明記した。

---

## 6. 不確実性評価の方法

moving block bootstrap（`block_bootstrap_mean_diff`）。

| 項目 | 設定 |
|---|---|
| 反復回数 | B = 1,000 |
| ブロック長 | `max(21, 2h)` → h=1で21営業日、h=21で42営業日 |
| ブロック長の根拠 | h営業日先予測では目的変数がh日重複するため、**ブロック長がh未満だと重複構造を壊す**。h=1では残差の短期自己相関を保持するため約1ヶ月を採用 |
| 対象 | Winkler score差、カバレッジ差 |
| 比較ペア | A−B、A−C、A−D、B−C（Linear・GBMの2モデル、h=1・h=21） |
| 判定 | 95%CIが0をまたぐ場合は「明確な差なし」と記述 |
| シード | 42（固定） |

---

## 7. LOCOの修正内容

1. Welch-t検定のp値を有意性の根拠として扱わない旨を、**スクリプトの実行時出力**と全ドキュメントに明記。
2. **エピソード単位の推論を新規実装**: 1エピソード=1観測として集計し、Normal群平均を上回るかの符号検定（両側二項検定）を実施。
3. 結果: 9エピソード中8個が上回り、**p = 0.0391**。中央値 +0.1502、範囲 [+0.0193, +0.3326]。
4. n=9の検出力が低いこと、日次検定（過大評価）と符号検定（保守的）の間に真の不確実性があることを明記。**どちらか一方を「正しい有意性」として採用しない**方針を宣言。

---

## 8. データパイプラインの修正内容

- **スナップショット読み込みを既定化**。生データ3列のみ読み、派生列は毎回再計算（パイプラインのロジック自体を検証するため）。
- `--source online` を明示した場合のみ yfinance から取得。`--refresh-snapshot` でCSV更新。
- 使用したデータソースを実行時に必ず表示。
- `validate_raw()`: 昇順検証、重複日付検出、必須列検証、値域検証、欠損数の記録。
- `pathlib` によるパス定義（cwd非依存）。
- 126営業日先であり暦日6ヶ月ではないこと、金利のみ休場日の扱いをdocstringに明記。

---

## 9. テスト一覧と結果

**24件すべて通過**（`python -m pytest tests/ -v`、外部API接続なし、実行時間 7.3秒）。

| 分類 | テスト | 結果 |
|---|---|---|
| データ品質 | スナップショットの存在と読み込み | PASS |
| | 必須列の存在 | PASS |
| | 日付インデックスが昇順・重複なし | PASS |
| | VIX/SP500/金利の値域 | PASS |
| | 検証関数が不正データを弾く | PASS |
| 先読み | Return_6M が正確に126行先を参照 | PASS |
| | target[i] == VIX[i+h]（h=1, 21） | PASS ×2 |
| | 特徴量に将来情報が入らない（h=1, 21） | PASS ×2 |
| | walk-forward学習データのtargetが実現済み（h=1, 21） | PASS ×2 |
| | 学習・較正・評価期間が重複しない | PASS |
| ACI | フィードバックがh営業日遅延する | PASS |
| | RollingFixedがαを更新しない | PASS |
| | 発行時点のクリップ後区間で被覆判定する | PASS |
| | 外れ続けるとαが下がる | PASS |
| | キューの取りこぼし・二重処理がない | PASS |
| 指標 | Winkler scoreが既知の例で正しい | PASS |
| | evaluate_intervalのカバレッジ計算 | PASS |
| | ブロックブートストラップが差を正しく検出/非検出 | PASS |
| 構成 | 図の出力ファイル名が一意 | PASS |
| | 主要結果ファイルが生成される | PASS |
| | **READMEの主要数値と結果ファイルが一致** | PASS |

**注**: 作業途中で `test_readme_key_numbers_match_results` は一度FAILした（READMEに旧数値 90.04 / 1.359 が残っていたため）。README更新後にPASSしている。この失敗は隠していない。

---

## 10. lint結果

`python -m flake8 .` → **指摘0件**（設定: `max-line-length=135`、E402除外＝`matplotlib.use("Agg")` を pyplot import 前に呼ぶ必要があるため）。

未使用import 8件、E302/E127/E501 を修正済み。

---

## 11. 再実行したコマンド

```bash
python -m flake8 .                          # 指摘0件
python analysis_01_phase_tests.py           # exit 0
python analysis_02_leave_one_crisis_out.py  # exit 0
python analysis_03_hac_power.py             # exit 0
python analysis_04_stationarity_economic.py # exit 0
python analysis_05_figure.py                # exit 0
python conformal_vix_forecast.py            # exit 0
python vix_analysis.py                      # exit 0
python build_key_results.py                 # exit 0
python -m pytest tests/ -q                  # 24 passed
```

全てスナップショット（オフライン）で実行。**オンライン再取得は一度も行っていない。**

---

## 12. 再生成した成果物

- `data/results_01〜05_*.json`（全て更新）
- `docs/key_results.md`（自動生成、253行）
- `figures/phase_returns_daily_vs_thinned.png`（旧 vix_phase_returns_fixed.png、2パネル）
- `figures/phase_returns_overview.png`（新規、1パネル）
- `figures/conformal_aci_overview.png`（旧 conformal_vix_aci.png、4手法の幅を重ねる形に変更）
- `figures/conformal_crisis_zoom.png`（4手法表示に変更）
- `figures/conformal_efficiency.png`（4手法表示に変更）
- `figures/vix_analysis_2008_zoom.png`（4手法表示に変更）

---

## 13. 修正前後の主要数値

| 指標 | 修正前 | 修正後 | 変化の理由 |
|---|---:|---:|---|
| ANOVA F（日次） | 182.6579 | 182.6579 | 変化なし |
| Durbin-Watson | 0.0323 | 0.0323 | 変化なし |
| GBM MAE (h=1) | 1.359 | **1.369** | 先読み修正（学習データ1行減） |
| GBM MAE (h=21) | 4.496 | **4.661** | 先読み修正（学習データ21行減） |
| Persistence MAE (h=1) | 1.079 | 1.079 | 変化なし（学習不要） |
| Linear MAE (h=21) | 3.600 | 3.611 | 先読み修正 |
| ACI カバレッジ (h=1, GBM) | 90.04% | **90.13%** | ACI更新式の修正＋先読み修正 |
| ACI Winkler (h=1, GBM) | 9.310 | 9.376 | 同上 |
| ACI Panicカバレッジ (h=1) | 82.1% | **83.2%** (153/184) | 同上 |
| FixedSplit Panicカバレッジ | 19.6% | 19.6% (36/184) | 変化なし |
| ACI 危機後カバレッジ (7ep平均) | 62.6% | **63.3%** | 同上 |
| **RollingFixed（新規）** | — | h=1 GBM: 89.84% / Winkler 13.457 | 新規追加 |
| **エピソード符号検定（新規）** | — | 9中8、**p=0.0391** | 新規追加 |
| **ブートストラップCI（新規）** | — | 全ペアで算出 | 新規追加 |

---

## 14. 修正によって変わった結論

### (a) 「h=21ではACIの優位性が消え、Linear+Fixedが全体最良」→ **撤回**

ブートストラップの結果、h=21では**ほぼ全ての手法差の95%CIが0をまたぐ**。正しくは「この標本設計では手法差を統計的に区別できない」。唯一の例外はGBMのACI vs Naiveで、**ACIが有意に劣る**（Winkler差 +4.724、95%CI [+2.176, +7.318]）。

### (b) 「Linear+ACIが全9通り中最良（7.756）」→ **撤回**

順位の断定を削除。ただしA vs B（αの適応の効果）はh=1でCIが0を含まず、**この差については明確に主張できる**ようになった（Linear: −0.660 [−1.251, −0.279]、GBM: −4.080 [−7.976, −1.411]）。

### (c) 新しい発見: **ACIの利点は「αの適応」由来であり、「窓の更新」由来ではない**

B vs C（窓の更新のみ）のWinkler差は、Linear・GBMともCIが0をまたぐ（明確な差なし）。一方でカバレッジは有意に改善する（+2.1〜2.8pt）。**窓の更新はカバレッジを目標に近づけるが、区間幅とのバランスまでは改善しない。**

Panic局面ではこの分離がさらに明確で、ACI 83.2% に対し RollingFixed は 48.9% まで崩れる。**危機局面でのカバレッジ維持は、ほぼαの適応の寄与である。**

### (d) 「LOCOで有意性が維持された」→ **限定**

日次p値を根拠から外し、エピソード単位の符号検定（9中8、p=0.0391）に置き換えた。**p=0.039は0.05の近傍であり、n=9の検出力も低い**ため、「強い証拠」とは言えない。効果の符号と大きさが特定の危機に依存していないことは確認できたが、それ以上ではない。

### (e) h=21のpre/post逆転 → **仮説から検証済みの事実へ**

対象日基準で再集計したところ逆転は解消した（ACI: pre 85.8% → post 43.2%）。当初「考えられる」としていた説明が支持された。

---

## 15. 未解決事項

| 項目 | 理由 |
|---|---|
| git初期化・GitHubへのpush | 指示により実行していない。`.gitignore` は準備済み |
| ACIハイパーパラメータ（γ、較正窓）の感度分析 | 実施すると「ACIが良く見える設定を探す」誘因が生じるため意図的に未実施。固定値であることを明記する方針を選択 |
| 再学習間隔252日の感度分析 | 同様に未実施。理由を明記 |
| 閾値・窓幅の感度分析 | 今回の制約「後付け分析を増やさない」に従い未実施。限界として明記 |
| 配当込みトータルリターンでの再計算 | 新規データソースが必要。限界と今後の課題に記載 |
| 「間引きで有意差が消える」の分解 | 自己相関の除去と検出力の喪失のどちらの寄与が大きいかを分離できていない。限界として明記 |

---

## 16. 研究上の限界として記載したもの

- 重複観測（実効独立標本数は9〜203の間）
- エピソード単位推論のn=9という小標本
- 検出力不足（Semiannual N=67で power=0.283）
- 価格リターンであり配当を含まない
- 閾値・窓幅・ホライズン・再学習間隔の事後選択の可能性（記録が残っていない）
- GBMのハイパーパラメータ未調整
- h=1のVIX水準予測が課題として易しい
- FixedSplitが極端な設定であること
- 単一市場・単一期間
- ACIが原論文の厳密な再現ではないこと（ローリング窓・下限クリップ）
- 周辺カバレッジであり条件付きカバレッジは保証しないこと
- 危機発生日の定義（Panic初日）の限界

---

## 17. 公開前に本人が確認すべき内容

**優先順位順**

1. **`conformal_vix_forecast.py` の `run_rolling_conformal()` 全行**（134-180行付近）。ACIの中核。キューによる遅延処理、被覆判定、α更新を説明できること。
2. **`block_bootstrap_mean_diff()`** とブロック長の選択理由。「なぜ21/42か」は必ず聞かれる。
3. **`walk_forward_forecast()` の `train_end = start - h + 1`**。先読み修正の中身。
4. **Winkler scoreの定義**と、なぜカバレッジ単独では不十分か。
5. **4手法の設計（A/B/C/Dの違い）**と、A vs B・B vs C が何を分離しているか。
6. **エピソード単位の符号検定**（`analysis_02`）と、なぜ日次p値を使わないのか。
7. `data_pipeline.py` のスナップショット読み込みと検証関数。
8. **README「AI利用について」節の記述が、本人の実感と一致しているか**。事実と違う部分があれば必ず修正すること。
9. 24件のテストが何を守っているか。

---

## 18. 最終公開判定

## **軽微な手動確認後に公開可能**

### 判定理由

**修正が完了している点**

- 前回レビューの「公開前に修正必須」6件すべてを、文言の弱化ではなく**実装レベルで修正**した。
- 「公開後でも改善可能」「中」「小」の指摘も、新規分析の追加を伴わないものはすべて対応した。
- 24件のテストが通り、flake8の指摘は0件、全スクリプトがスナップショットからexit 0で再実行できる。
- READMEの数値と結果ファイルの一致が**テストによって保証される**構造になった。
- 修正の結果、**結論が2つ撤回・1つ追加**された。これらは隠さず本報告書とREADMEに記載した。
- 不確実性の定量化が入ったことで、前回最大の弱点だった「点推定の順位を断定する」問題が解消した。

**「このまま公開可能」としない理由**

公開前に**本人にしかできない確認**が2点残っている。

1. **README「AI利用について」節の事実確認。** この節は本セッションの経緯に基づいて記述したが、本人の実際の作業範囲と一致しているかは本人しか判断できない。事実と異なる記述で公開すると、AI利用を開示した意図とは逆に信頼性を損なう。
2. **本報告書§17の理解確認。** 特にACIの更新ロジックとブロックブートストラップは、本人が説明できない状態で公開すると面接で不利に働く。コードが正しく動くことと、本人が説明できることは別問題である。

この2点は技術的な欠陥ではなく、**ポートフォリオとして提示する際の本人の準備**の問題であるため、「軽微な手動確認後」とした。

技術的な観点では、**現時点で公開して統計・時系列の専門家のレビューに耐えうる状態**にあると判断する。
