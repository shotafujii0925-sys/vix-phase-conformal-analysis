[flake8]
max-line-length = 135
exclude = .git,__pycache__,.pytest_cache,archived
# E402: module level import not at top of file
# (matplotlib.use("Agg") must be called before importing pyplot)
extend-ignore = E402

[tool:pytest]
testpaths = tests
